@{
    ViewData["Title"] = "Login";
}
<h2>@ViewData["Title"]</h2>

<ul>
    @foreach (var claim in User.Claims)
    {
        <li>@claim.Type: @claim.Value</li>
    }
</ul>
<a asp-area="" asp-controller="Home" asp-action="Address">Show Address</a>